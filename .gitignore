# Dependencies & builds
node_modules/
dist/
build/
*.tsbuildinfo

# Env & secrets (never commit)
.env
.env.*
!.env.example

# Logs & OS
*.log
npm-debug.log*
.DS_Store
Thumbs.db

# IDE / local tools
.idea/
.cursor/
*.swp
*.swo

# Keys & local certs (never commit)
*.pem
*.key
*-credentials.json
google-service-account*.json

# Test / coverage
coverage/

# Backups (if created under project)
backups/
