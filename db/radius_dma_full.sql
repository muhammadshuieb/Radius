-- Replace this file with your full DMA MySQL dump for database `radius`
-- (e.g. phpMyAdmin export or: mysqldump -uUSER -p radius > radius_dma_full.sql).
-- Then: docker compose -f docker-compose.mysql.yml up -d mysql
--
-- Placeholder so Docker init does not fail on empty repo:
SET NAMES utf8mb4;
SELECT 1 AS dma_placeholder;
